---

---

<header class="col-12 col-lg-2 flex-grow-1 header-area">
		<div id="alwaysShow" class="d-flex flex-row flex-nowrap justify-content-between align-items-center w-100">
			<div class="logo my-5">
					<a><img class="logo" src="/assets/images/core-img/green-haven-logo.png" alt="Green Haven Logo'"></a>
			</div>
			<div id="hamburger" class="hamburger">
				<span></span>
				<span></span>
				<span></span>
			</div>
		</div>
		<nav class="nav collapsed" id="menu">
			<ul class="list-unstyled ">
				<li class="active"><a class="d-block text-decoration-none text-uppercase py-4" href="/">Home</a></li>
				<li><a class="d-block text-decoration-none text-uppercase py-4" href="/products/page">Shop</a></li>
				<li><a class="d-block text-decoration-none text-uppercase py-4" href="/cart.html">Cart</a></li>
				<li><a class="d-block text-decoration-none text-uppercase py-4" href="#">Checkout</a></li>
			</ul>
		</nav>
		<div class="nav-btn-group b-0 d-flex flex-column">
			<a style="background-color: #224229;" href="#" class="btn nav-btn mb-3">Discount</a>
			<a style="background-color: #131212;" href="#" class="btn nav-btn active mb-5">New this week</a>
		</div>
		<div class="cart-fav-search d-flex justify-content-around d-lg-block mb-100">
			<a href="cart.html" class="cart-nav nav-icon-btns text-decoration-none text-uppercase py-2"><img src="/assets/images/core-img/cart.png" alt=""> Cart <span class="snipcart-items-count">(0)</span></a>
			<a href="#" class="fav-nav nav-icon-btns text-decoration-none text-uppercase py-2"><img src="/assets/images/core-img/favorites.png" alt=""> Favorites</a>
			<a href="#" class="search-nav nav-icon-btns text-decoration-none text-uppercase py-2"><img src="/assets/images/core-img/search.png" alt=""> Search</a>
		</div>
</header>

<script >
  document.getElementById("hamburger").addEventListener("click", function () {
    toggleMenu();
    //animateHamburger();
  });

  function toggleMenu() {
    var menu = document.getElementById("menu");
    if (menu.classList.contains("collapsed")) {
      menu.classList.remove("collapsed");
			menu.classList.add("expanded");
    } else if (menu.classList.contains("expanded")) {
      menu.classList.remove("expanded");
      menu.classList.add("collapsed");
    }
  }

  function animateHamburger() {
    var hamburger = document.getElementById("hamburger");
    hamburger.classList.toggle("active");
  }

  var menuElement = document.getElementById("menu");
  var menuItems = Array.from(document.querySelectorAll("#menu li")) as HTMLLIElement[];
  var animationDelay = .15; // Delay between each menu item animation

  function animateMenuItems() {
    menuItems.forEach(function(item, index) {
      item.style.animation = `expandAnimation 0.5s forwards ${index * animationDelay}s`; // Change animation time
    });
  }

  function resetMenuItems() {
    menuItems.forEach(function(item) {
      item.style.animation = "";
    });
  }

  menuElement.addEventListener("transitionend", function() {
    if (menuElement.classList.contains("expanded")) {
      //animateMenuItems();
    } else if (menuElement.classList.contains("collapsed")) {
      resetMenuItems();
    }
  });
</script>

<style>
	.logo { max-width: 200px; }
	.header-area { background-color: #fff; }
	.nav-btn-group {
		/*max-width: 150px;*/
	}
	.header-area .nav li a {
		font-size: 1.4rem;
		line-height: 0.9;
		color: var(--main-font-color);
	}
	.nav-btn-group {
		font-size: 1.8rem;
		line-height: 56px;
		font-weight: 400;
	}
	.nav-btn {
		color: #ffffff;
    padding: 10px 5px;
		max-width: 225px;
		font-size: 1.8rem;
		font-weight: 400;
		display: flex;
		align-items: center;
		justify-content: center;
	}
	.nav-icon-btns {
		display: block;
		font-size: 1.4rem;
		color: var(--main-font-color);
	}
@media only screen and (max-width: 768px) {

	/* Hamburger */
	.hamburger {
		display: inline-block;
		cursor: pointer;
		padding: 10px;
	}
	.hamburger span {
		display: block;
		width: 30px;
		height: 3px;
		background-color: var(--main-font-color);
		margin-bottom: 5px;
		transition: transform 0.3s ease-in-out, opacity 0.3s ease-in-out;
	}
	.hamburger.active span:first-child {
		transform: translateY(8px) rotate(45deg);
	}
	.hamburger.active span:nth-child(2) {
		opacity: 0;
	}
	.hamburger.active span:last-child {
		transform: translateY(-8px) rotate(-45deg);
	}

	/* Hamburgers are better animated */
  .hamburger span {
    /* Existing styles */
    transition: transform 0.3s ease-in-out;
  }

  .hamburger.active span:first-child {
    transform: translateY(8px) rotate(45deg);
  }

  .hamburger.active span:nth-child(2) {
    transform: scale(0);
  }

  .hamburger.active span:last-child {
    transform: translateY(-8px) rotate(-45deg);
  }

  .expanded {
    max-height: 500px; /* Set the maximum height to a suitable value */
    transition: max-height 0.3s ease-in-out;
  }

  .collapsed {
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.3s ease-in-out;
  }

  .nav li {
		/*transform: translateX(-200%);*/
    animation-duration: 0.025s;
    animation-fill-mode: both;
    animation-timing-function: ease-in;
  }

  /* Animation for expanding menu items */
  @keyframes expandAnimation {
    0% {
      opacity: 0;
      transform: translateY(10px);
    }
    50% {
      opacity: 1;
      transform: translateY(5px);
    }
    100% {
      transform: translateY(0);
    }
  }

  /* Animation for collapsing menu items */
  @keyframes collapseAnimation {
    from {
      opacity: 1;
      transform: translateY(0);
    }
    to {
      opacity: 0;
      transform: translateY(-10px);
    }
  }
}
</style>